# Project Rules & Preferences

## Git Workflow
- **NEVER** push code to GitHub (`git push`) without explicit prior request or confirmation from the user.
- All code edits and tests must remain strictly local on the machine until the user specifically asks to push.
